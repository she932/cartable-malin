# Copie ce fichier en ".env.local" pour tester en local (jamais à partager)
# En production (Vercel), cette valeur se met dans Project Settings > Environment Variables
ANTHROPIC_API_KEY=colle_ta_cle_ici
